# Quicksort Summary

Quicksort is a divide-and-conquer sorting algorithm that selects a pivot element, partitions the array so elements smaller than the pivot go left and larger go right, then recursively sorts both partitions.
It achieves O(n log n) average time complexity with O(log n) space complexity, making it one of the fastest general-purpose sorting algorithms in practice.
